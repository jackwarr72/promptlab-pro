[build]
  command = "npm run build"
  publish = "dist"

# Redirect /api/generate to the Netlify Function
[[redirects]]
  from = "/api/generate"
  to = "/.netlify/functions/generate"
  status = 200
  force = true
  conditions = { Language = ["*"] }
